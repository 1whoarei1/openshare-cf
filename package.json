{
  "name": "openshare-cf",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-types": "wrangler types",
    "check": "node --check src/index.js && node --check public/app.js && node scripts/check-project.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
